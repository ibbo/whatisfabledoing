server {
    listen 80;
    listen [::]:80;
    server_name api.whatisfabledoing.com;

    location / {
        proxy_pass http://127.0.0.1:8127;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header Host $host;
    }
}
